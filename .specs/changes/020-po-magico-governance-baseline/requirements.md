# Requirements

- Record the active Pó Mágico version, repository, entry file, and audited commit.
- Add a concise, English operational context for future Codex work.
- Preserve the existing `.specs/changes/` structure and historical records.
- Define evidence-led GPT/Codex model routing, reasoning escalation, fallback, checkpoint, and reporting rules.
- Record only the actual model/runtime facts available to the agent.
- Do not change app code, database schema, environment variables, content, analytics collection, deployment, or production state.
