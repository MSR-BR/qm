# Notes

- Termo supplies useful visual and information-architecture references, but it currently stores its bookmark through authentication metadata. QM needs row-level progress because it must aggregate section completion and continuation privately and reliably.
- The existing QM favorites remain authentication metadata and saved exercises remain in `qm_saved_exercises`; this change preserves both paths.
