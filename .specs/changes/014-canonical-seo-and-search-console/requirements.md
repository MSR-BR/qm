# Requirements

- Begin only after C13 final-domain routing is stable.
- Update canonical URLs, Open Graph URLs, JSON-LD URLs, sitemap URLs, robots sitemap reference, and search action targets to the final host.
- Keep Chapters 8–13 excluded from search, exercises, and public sitemaps.
- Verify the final host in Google Search Console and submit the final sitemap index.
- Do not alter Search Console ownership or submit properties without user authorization.
