# Requirements

1. Add an English public landing page that exposes only published Chapters 1–7 and the simulator catalogue.
2. Keep `index.html` as the reading application; do not break existing app query routes.
3. Add canonical, robots, and structured-data metadata for the public landing.
4. Split sitemap output into a sitemap index, public app routes, and content pages.
5. Locked chapters remain absent from all public discovery artifacts.
