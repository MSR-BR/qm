(() => {
  const root=document.querySelector("[data-simulator-slug]"); if(!root) return;
  const slug=root.dataset.simulatorSlug||new URLSearchParams(location.search).get("sim"),$=id=>document.getElementById(id);root.dataset.simulatorSlug=slug||"";
  const H=6.62607015e-34,HBAR=H/(2*Math.PI),C=299792458,KB=1.380649e-23,E=1.602176634e-19,ME=9.1093837139e-31,EPS0=8.8541878128e-12,SIGMA=5.670374419e-8;
  const colors={green:"#2f6b4f",rust:"#a64b35",blue:"#376b8c",gold:"#d5b260",grid:"#dbe5dd",ink:"#26332c",muted:"#6b756f"};
  const configs={
    "blackbody-radiation":{title:"Blackbody Radiation Lab",subtitle:"Compare the Planck and Rayleigh–Jeans spectral radiances R(f,T), using the notation and frequency representation of the book.",plot:"Spectral radiance as a function of frequency",controls:[r("temperature","Temperature T",1000,12000,100,5800,"K"),r("compareTemperature","Comparison T₂",0,12000,100,3000,"K")],metrics:[["Peak frequency <span class=\"case-sensitive\">f</span>ₘₐₓ","peak"],["Total power <span class=\"case-sensitive\">σ</span>T⁴","power"],["Peak spectral radiance R(<span class=\"case-sensitive\">f</span>ₘₐₓ,T)","radiance"],["Visible-band fraction","visible"]],equations:String.raw`\[\frac{du}{df}=\frac{8\pi h}{c^3}\frac{f^3}{e^{hf/(k_BT)}-1}\]\[R(f,T)=\frac{c}{4}\frac{du}{df}=\frac{2\pi h}{c^2}\frac{f^3}{e^{hf/(k_BT)}-1}\]\[M=\sigma T^4,\qquad \sigma=5.670374419\times10^{-8}\ \mathrm{W\,m^{-2}\,K^{-4}}\]`},
    "double-slit":{title:"Double-Slit Interference",subtitle:"Change the SI-valued optical geometry and connect the experimental arrangement to the diffraction-modulated interference pattern.",plot:"Experimental arrangement and screen intensity",controls:[r("wavelength","Wavelength λ",3.8e-7,7.5e-7,5e-9,5.5e-7,"m"),r("separation","Slit separation d",2e-6,2e-5,1e-7,8e-6,"m"),r("slitWidth","Slit width a",5e-7,5e-6,1e-7,2e-6,"m"),r("distance","Screen distance L",.5,3,.05,1.5,"m")],metrics:[["Fringe spacing Δy","fringe"],["First diffraction minimum y₁","minimum"],["Wavelength λ","wave"],["Path condition","condition"]],equations:String.raw`\[I(\theta)=I_0\cos^2\!\left(\frac{\pi d\sin\theta}{\lambda}\right)\operatorname{sinc}^2\!\left(\frac{\pi a\sin\theta}{\lambda}\right)\]\[\Delta y\simeq\frac{\lambda L}{d},\qquad y_1\simeq\frac{\lambda L}{a}\]\[\lambda,\ a,\ d,\ L,\ y\ \text{are expressed in SI units (m).}\]`},
    "hydrogen-bohr":{title:"Hydrogen Spectrum and Bohr Model",subtitle:"Select a hydrogen transition and connect SI energy, orbital radii and the emitted photon; the eV-to-joule conversion is shown explicitly.",plot:"Energy levels and emitted photon",controls:[r("initialN","Initial level nᵢ",2,8,1,3,""),r("finalN","Final level n_f",1,7,1,2,"")],metrics:[["Photon energy ΔE","energy"],["Wavelength λ","wavelength"],["Spectral region","region"],["Initial Bohr radius rₙ","radius"]],equations:String.raw`\[E_n=-\frac{2.1798723611\times10^{-18}\ \mathrm{J}}{n^2},\qquad r_n=a_0n^2\]\[\Delta E=\frac{hc}{\lambda},\qquad \frac{1}{\lambda}=R_H\left(\frac{1}{n_f^2}-\frac{1}{n_i^2}\right)\]\[1\ \mathrm{eV}=1.602176634\times10^{-19}\ \mathrm{J},\qquad E[\mathrm J]=E[\mathrm{eV}]\,(1.602176634\times10^{-19})\]`},
    "thomson-circle":{title:"Electron e/m — Circular-Beam Tube",subtitle:"Set the Helmholtz coils (R, N and I), calculate B, set Vₐ, then measure the beam radius to determine e/m.",plot:"Circular beam and radius ruler",controls:[r("coilRadius","Coil radius R",10,25,.1,15,"cm"),r("coilTurns","Turns per coil N",80,180,1,130,""),r("coilCurrent","Coil current I",.5,3,.01,1.5,"A"),r("acceleratingVoltage","Accelerating voltage Vₐ",100,400,5,250,"V")],metrics:[["Calculated magnetic field B","field"],["Electron speed from Vₐ","speed"],["Measured beam radius r","radius"],["Experimental e/m","ratio"]],equations:String.raw`\[\text{1. Helmholtz coils:}\qquad B=\mu_0\left(\frac45\right)^{3/2}\frac{NI}{R}\]\[\text{2. Acceleration through }V_a:\qquad eV_a=\frac12mv^2\]\[v^2=2\frac{e}{m}V_a\]\[\text{3. Circular motion:}\qquad evB=\frac{mv^2}{r}\]\[\frac{e}{m}=\frac{v}{Br}\]\[\text{4. Eliminate }v:\qquad \left(\frac{e}{m}\right)^2=\frac{2(e/m)V_a}{B^2r^2}\]\[\boxed{\frac{e}{m}=\frac{2V_a}{B^2r^2}}\]`},
    "thomson-deflection":{title:"Electron e/m — Selector and Deflection",subtitle:"The controls give the magnitudes of the three fields in SI units: E(yA), B(z) and E(yB). Tune the selector until the beam is horizontal, then measure its electric deflection.",plot:"Velocity selection followed by electric deflection",controls:[r("selectorField","Magnitude E(yA)",5000,30000,100,15000,"V/m"),r("selectorMagnetic","Magnitude B(z)",2e-4,2e-3,1e-5,1e-3,"T"),r("deflectionField","Magnitude E(yB)",1000,10000,100,5000,"V/m")],metrics:[["Selected speed vₓ","speed"],["Measured deflection d","deflection"],["Experimental |q|/m","ratio"]],equations:String.raw`\[E_{yA}\equiv|\mathbf E_{yA}|,\qquad B_z\equiv|\mathbf B_z|,\qquad E_{yB}\equiv|\mathbf E_{yB}|\]\[|q|E_{yA}=|q|v_xB_z,\qquad v_x=\frac{E_{yA}}{B_z}\]\[l=4.00\times10^{-2}\ \mathrm m,\qquad L=1.20\times10^{-1}\ \mathrm m\quad\text{(fixed)}\]\[\frac{|q|}{m}=\frac{2d\,v_x^2}{E_{yB}\,l(l+2L)}\]`},
    "millikan-oil-drop":{title:"Millikan Oil-Drop Experiment",subtitle:"Observe the same drop falling with the field off and rising with the field on. The measured terminal speeds determine q = ne.",plot:"Measure one drop or run 500 automatically",controls:[r("plateGap","Known plate separation d",3,10,.1,5,"mm"),r("plateVoltage","Applied voltage V",100,1000,5,525,"V")],metrics:[["Measured fall speed v_f","fallMeasured"],["Measured rise speed v_r","riseMeasured"],["Charge from both measurements","charge"],["Quantization q = ne","nearest"]],equations:String.raw`\[mg=6\pi\eta Rv_f,\qquad R=\sqrt{\frac{9\eta v_f}{2\rho g}}\]\[|q|=6\pi\frac{d}{V}\sqrt{\frac{9\eta^3}{2\rho g}}(v_r+v_f)\sqrt{v_f}\]\[q=ne\]\[\text{Constants used:}\qquad \eta=1.81\times10^{-5}\ \mathrm{Pa\,s}\]\[\rho=860\ \mathrm{kg\,m^{-3}},\qquad g=9.80665\ \mathrm{m\,s^{-2}}\]\[e=1.602176634\times10^{-19}\ \mathrm{C},\qquad E=\frac{V}{d}\]`}
  };
  function r(id,label,min,max,step,value,unit){return{type:"range",id,label,min,max,step,value,unit}}
  function s(id,label,options,value){return{type:"select",id,label,options,value}}
  const cfg=configs[slug]; if(!cfg)return;
  $("simTitle").textContent=cfg.title;$("simSubtitle").textContent=cfg.subtitle;$("plotTitle").textContent=cfg.plot;document.title=`${cfg.title} | Quantum Mechanics`;
  $("controlFields").innerHTML=cfg.controls.map(control=>control.type==="select"?`<div class="control-group"><label class="control-label" for="${control.id}">${control.label}</label><select id="${control.id}">${control.options.map(([value,label])=>`<option value="${value}"${value===control.value?" selected":""}>${label}</option>`).join("")}</select></div>`:`<div class="control-group"><div class="control-row"><label class="control-label" for="${control.id}">${control.label}</label><output class="control-value" id="${control.id}Value"></output></div><input id="${control.id}" type="range" min="${control.min}" max="${control.max}" step="${control.step}" value="${control.value}"></div>`).join("");
  if(slug==="millikan-oil-drop")$("controlFields").insertAdjacentHTML("beforeend",`<div class="button-row"><button class="sim-button" id="measureFallButton" type="button">1. Observe fall</button><button class="sim-button" id="measureRiseButton" type="button">2. Turn field on</button><button class="sim-button secondary" id="runHundredButton" type="button">Run 500 drops</button></div>`);
  $("metricPanel").insertAdjacentHTML("beforeend",cfg.metrics.map(([label,id])=>`<div class="metric"><div class="metric-label">${label}</div><div class="metric-value" id="${id}Metric"></div></div>`).join(""));$("equationBox").innerHTML=cfg.equations;window.MathJax?.typesetPromise?.([$("equationBox")]);
  const format=(v,d=2)=>Number(v).toLocaleString("pt-BR",{maximumFractionDigits:d,minimumFractionDigits:d});
  const sci=(v,d=3)=>Number(v).toExponential(d).replace(".",",");
  function state(){const out={};cfg.controls.forEach(c=>out[c.id]=c.type==="range"?Number($(c.id).value):$(c.id).value);return out}
  function updateOutputs(s){cfg.controls.filter(c=>c.type==="range").forEach(c=>{const value=s[c.id],shown=value!==0&&Math.abs(value)<1e-3?sci(value,4):format(value,c.step<.01?3:c.step<1?2:0);$(`${c.id}Value`).textContent=`${shown}${c.unit?` ${c.unit}`:""}`})}
  function fit(){const canvas=$("primaryCanvas"),ratio=Math.max(1,devicePixelRatio||1),rect=canvas.getBoundingClientRect(),width=Math.max(300,Math.round(rect.width)),height=Math.max(300,Math.round(rect.height));canvas.width=width*ratio;canvas.height=height*ratio;const ctx=canvas.getContext("2d");ctx.setTransform(ratio,0,0,ratio,0,0);ctx.clearRect(0,0,width,height);return{ctx,width,height}}
  function axes(ctx,w,h,p,xLabel,yLabel){ctx.strokeStyle=colors.grid;ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(p.l,p.t);ctx.lineTo(p.l,h-p.b);ctx.lineTo(w-p.r,h-p.b);ctx.stroke();ctx.fillStyle=colors.muted;ctx.font="11px Inter";ctx.fillText(yLabel,p.l+5,p.t+12);ctx.fillText(xLabel,w-p.r-60,h-8)}
  const set=(id,value)=>$(`${id}Metric`).textContent=value;
  function drawBlackbody(s){const{ctx,width:w,height:h}=fit(),p={l:58,r:20,t:24,b:38},fMin=1e12,fMax=3e15,planck=(f,T)=>{const z=H*f/(KB*T);return 2*Math.PI*H*f**3/(C*C*Math.expm1(Math.min(z,700)))},rayleigh=(f,T)=>2*Math.PI*KB*T*f*f/(C*C);let max=0;for(let i=0;i<=900;i++){const f=fMin+(fMax-fMin)*i/900;max=Math.max(max,planck(f,s.temperature),s.compareTemperature?planck(f,s.compareTemperature):0)}axes(ctx,w,h,p,"f (Hz)","R(f,T)");const x=f=>p.l+(f-fMin)/(fMax-fMin)*(w-p.l-p.r),yOf=q=>h-p.b-q/max*(h-p.t-p.b)*.9,fVisibleMin=C/7.5e-7,fVisibleMax=C/3.8e-7;ctx.fillStyle="rgba(213,178,96,.15)";ctx.fillRect(x(fVisibleMin),p.t,x(fVisibleMax)-x(fVisibleMin),h-p.t-p.b);[[s.temperature,colors.green],[s.compareTemperature,colors.blue]].forEach(([T,c])=>{if(!T)return;ctx.strokeStyle=c;ctx.lineWidth=2.5;ctx.beginPath();for(let i=0;i<=900;i++){const f=fMin+(fMax-fMin)*i/900,y=yOf(planck(f,T));i?ctx.lineTo(x(f),y):ctx.moveTo(x(f),y)}ctx.stroke()});ctx.save();ctx.beginPath();ctx.rect(p.l,p.t,w-p.l-p.r,h-p.t-p.b);ctx.clip();ctx.strokeStyle=colors.rust;ctx.setLineDash([6,4]);ctx.beginPath();for(let i=0;i<=900;i++){const f=fMin+(fMax-fMin)*i/900,y=yOf(rayleigh(f,s.temperature));i?ctx.lineTo(x(f),y):ctx.moveTo(x(f),y)}ctx.stroke();ctx.restore();ctx.setLineDash([]);const legendX=p.l+8;ctx.fillStyle=colors.green;ctx.fillText(`Planck ${format(s.temperature,0)} K`,legendX,p.t+35);ctx.fillStyle=colors.blue;if(s.compareTemperature)ctx.fillText(`Planck ${format(s.compareTemperature,0)} K`,legendX,p.t+50);ctx.fillStyle=colors.rust;ctx.fillText("Rayleigh–Jeans",legendX,p.t+65);ctx.fillStyle=colors.muted;ctx.fillText("visible",x(fVisibleMin)+4,h-p.b-8);const peak=2.821439372*KB*s.temperature/H;set("peak",`${sci(peak,4)} Hz`);set("power",`${sci(SIGMA*s.temperature**4)} W/m²`);set("radiance",`${sci(planck(peak,s.temperature))} W·m⁻²·sr⁻¹·Hz⁻¹`);let vis=0,total=0;for(let i=0;i<=4000;i++){const f=fMin+(fMax-fMin)*i/4000,q=planck(f,s.temperature);total+=q;if(f>=fVisibleMin&&f<=fVisibleMax)vis+=q}set("visible",`${format(100*vis/total,1)}%`)}
  function drawSlit(s){
    const{ctx,width:w,height:h}=fit(),lambda=s.wavelength,d=s.separation,a=s.slitWidth,L=s.distance,divider=w*.39,p={l:divider+42,r:20,t:28,b:38},yMax=.12,x=y=>p.l+(y+yMax)/(2*yMax)*(w-p.l-p.r);
    ctx.font="11px Inter";ctx.fillStyle=colors.muted;ctx.fillText("experimental arrangement",18,20);
    const sourceX=28,barrierX=divider*.48,screenX=divider-28,mid=h*.52,slitGap=Math.min(58,24+1200000*d),slitHeight=Math.min(25,8+2500000*a),topSlit=mid-slitGap/2,bottomSlit=mid+slitGap/2;
    ctx.strokeStyle=colors.gold;ctx.lineWidth=2;for(let j=-2;j<=2;j++){ctx.beginPath();for(let i=0;i<=55;i++){const px=sourceX+i*(barrierX-sourceX-10)/55,py=mid+j*12+5*Math.sin(i*.55);i?ctx.lineTo(px,py):ctx.moveTo(px,py)}ctx.stroke()}
    ctx.fillStyle=colors.muted;ctx.fillText(`λ = ${sci(lambda,3)} m`,sourceX,mid-45);
    ctx.strokeStyle=colors.ink;ctx.lineWidth=5;ctx.beginPath();ctx.moveTo(barrierX,28);ctx.lineTo(barrierX,topSlit-slitHeight/2);ctx.moveTo(barrierX,topSlit+slitHeight/2);ctx.lineTo(barrierX,bottomSlit-slitHeight/2);ctx.moveTo(barrierX,bottomSlit+slitHeight/2);ctx.lineTo(barrierX,h-52);ctx.stroke();
    ctx.strokeStyle=colors.grid;ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(screenX,28);ctx.lineTo(screenX,h-52);ctx.stroke();
    ctx.setLineDash([4,4]);ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(barrierX,topSlit);ctx.lineTo(screenX,mid);ctx.moveTo(barrierX,bottomSlit);ctx.lineTo(screenX,mid);ctx.stroke();ctx.setLineDash([]);
    const dimension=(x1,y1,x2,y2,label)=>{ctx.strokeStyle=colors.rust;ctx.lineWidth=1.5;ctx.beginPath();ctx.moveTo(x1,y1);ctx.lineTo(x2,y2);ctx.stroke();const ang=Math.atan2(y2-y1,x2-x1);[[x1,y1,ang],[x2,y2,ang+Math.PI]].forEach(([px,py,angle])=>{ctx.beginPath();ctx.moveTo(px,py);ctx.lineTo(px+7*Math.cos(angle+.45),py+7*Math.sin(angle+.45));ctx.moveTo(px,py);ctx.lineTo(px+7*Math.cos(angle-.45),py+7*Math.sin(angle-.45));ctx.stroke()});ctx.fillStyle=colors.rust;ctx.fillText(label,(x1+x2)/2+4,(y1+y2)/2-4)};
    dimension(barrierX+13,topSlit-slitHeight/2,barrierX+13,topSlit+slitHeight/2,`a = ${sci(a,2)} m`);dimension(barrierX+34,topSlit,barrierX+34,bottomSlit,`d = ${sci(d,2)} m`);dimension(barrierX,h-30,screenX,h-30,`L = ${format(L,2)} m`);
    ctx.fillStyle=colors.muted;ctx.fillText("double slit",barrierX-24,h-10);ctx.fillText("screen",screenX-16,h-10);ctx.strokeStyle=colors.grid;ctx.beginPath();ctx.moveTo(divider,18);ctx.lineTo(divider,h-18);ctx.stroke();
    axes(ctx,w,h,p,"screen y (m)","I/I₀");ctx.strokeStyle=colors.green;ctx.lineWidth=2.5;ctx.beginPath();for(let i=0;i<=900;i++){const y=-yMax+2*yMax*i/900,th=Math.atan(y/L),alpha=Math.PI*a*Math.sin(th)/lambda,beta=Math.PI*d*Math.sin(th)/lambda,I=Math.cos(beta)**2*(Math.abs(alpha)<1e-9?1:(Math.sin(alpha)/alpha)**2),py=h-p.b-I*(h-p.t-p.b)*.9;i?ctx.lineTo(x(y),py):ctx.moveTo(x(y),py)}ctx.stroke();set("fringe",`${sci(lambda*L/d,4)} m`);set("minimum",`${sci(lambda*L/a,4)} m`);set("wave",`${sci(lambda,4)} m`);set("condition","d sin θ = mλ")
  }
  function drawHydrogen(s){if(s.finalN>=s.initialN){s.finalN=Math.max(1,s.initialN-1);$("finalN").value=s.finalN}const{ctx,width:w,height:h}=fit(),ground=2.1798723611e-18,Ei=-ground/s.initialN**2,Ef=-ground/s.finalN**2,dE=Ei-Ef,lambda=H*C/dE,lambdaNm=lambda*1e9,p={l:58,r:w*.47,t:28,b:38},y=e=>p.t+(-e)/ground*(h-p.t-p.b),levelRight=w*.53;ctx.strokeStyle=colors.grid;ctx.beginPath();ctx.moveTo(p.l,p.t);ctx.lineTo(p.l,h-p.b);ctx.stroke();for(let n=1;n<=8;n++){const en=-ground/n**2,py=y(en);ctx.strokeStyle=n===s.initialN?colors.green:n===s.finalN?colors.rust:colors.grid;ctx.lineWidth=n===s.initialN||n===s.finalN?3:1;ctx.beginPath();ctx.moveTo(p.l,py);ctx.lineTo(levelRight,py);ctx.stroke();ctx.fillStyle=colors.muted;ctx.fillText(`n=${n}`,p.l+5,py-3)}ctx.strokeStyle=colors.gold;ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(levelRight-22,y(Ei));ctx.lineTo(levelRight-22,y(Ef));ctx.stroke();const cx=w*.77,cy=h*.53,maxN=8,orbitStep=Math.min(18,(Math.min(w*.4,h*.78)-30)/(2*maxN));ctx.fillStyle=colors.rust;ctx.beginPath();ctx.arc(cx,cy,8,0,2*Math.PI);ctx.fill();for(let n=1;n<=maxN;n++){ctx.strokeStyle=n===s.initialN?colors.green:n===s.finalN?colors.rust:colors.grid;ctx.lineWidth=n===s.initialN||n===s.finalN?2.5:1;ctx.beginPath();ctx.arc(cx,cy,n*orbitStep,0,2*Math.PI);ctx.stroke()}const point=(n,c)=>{ctx.fillStyle=c;ctx.beginPath();ctx.arc(cx+n*orbitStep,cy,6,0,2*Math.PI);ctx.fill()};point(s.initialN,colors.green);point(s.finalN,colors.rust);ctx.strokeStyle=colors.gold;ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(cx+s.initialN*orbitStep,cy-10);ctx.lineTo(cx+s.finalN*orbitStep,cy-10);ctx.stroke();ctx.fillStyle=colors.muted;ctx.fillText(`nᵢ=${s.initialN}`,cx+s.initialN*orbitStep-12,cy+22);ctx.fillText(`n_f=${s.finalN}`,cx+s.finalN*orbitStep-12,cy+22);ctx.fillText(`photon: λ = ${sci(lambda,3)} m`,cx-75,cy-h*.35);ctx.fillText(`ΔE = ${sci(dE,3)} J`,cx-75,cy-h*.35+16);ctx.fillText("1 eV = 1,602176634e−19 J",cx-75,cy-h*.35+32);const region=lambdaNm<380?"Ultraviolet":lambdaNm<=750?"Visible":"Infrared",series=({1:"Lyman",2:"Balmer",3:"Paschen",4:"Brackett"})[s.finalN]||"higher series";set("energy",`${sci(dE,4)} J`);set("wavelength",`${sci(lambda,4)} m`);set("region",`${region} · ${series}`);set("radius",`${sci(5.29177210903e-11*s.initialN**2,4)} m`)}
  function drawThomsonCircle(s){const{ctx,width:w,height:h}=fit(),mu0=4*Math.PI*1e-7,R=s.coilRadius/100,B=mu0*(4/5)**1.5*s.coilTurns*s.coilCurrent/R,accepted=E/ME,v=Math.sqrt(2*accepted*s.acceleratingVoltage),trueRadius=v/(accepted*B),measuredRadius=Math.round(trueRadius/.0005)*.0005,ratio=2*s.acceleratingVoltage/(B*B*measuredRadius*measuredRadius),cx=w/2,cy=h/2,scale=Math.min(w,h)*.34/.08,rp=Math.min(trueRadius,.08)*scale;ctx.fillStyle="rgba(55,107,140,.04)";ctx.fillRect(18,18,w-36,h-36);ctx.fillStyle="rgba(55,107,140,.42)";ctx.font="14px Inter";for(let y=54;y<h-24;y+=46)for(let x=48;x<w-24;x+=54)ctx.fillText("⊗",x,y);ctx.fillStyle="rgba(38,51,44,.78)";ctx.font="600 12px Inter";ctx.fillText(`uniform B into page = ${sci(B,4)} T`,30,35);ctx.strokeStyle=colors.green;ctx.lineWidth=4;ctx.beginPath();ctx.arc(cx,cy,rp,0,2*Math.PI);ctx.stroke();ctx.fillStyle=colors.gold;ctx.beginPath();ctx.arc(cx+rp,cy,7,0,2*Math.PI);ctx.fill();ctx.strokeStyle=colors.rust;ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(cx,cy);ctx.lineTo(cx+rp,cy);ctx.stroke();ctx.fillStyle=colors.rust;ctx.font="11px Inter";ctx.fillText(`measured r = ${format(measuredRadius*100,2)} cm`,cx+rp/4,cy-9);set("field",`${sci(B,4)} T`);set("speed",`${sci(v,4)} m/s`);set("radius",`${format(measuredRadius*100,2)} cm`);set("ratio",`${sci(ratio,4)} C/kg`)}
  function drawThomsonDeflection(s){const{ctx,width:w,height:h}=fit(),EA=s.selectorField,B=s.selectorMagnetic,EB=s.deflectionField,v0=1.5e7,vSelected=EA/B,selectorError=(vSelected-v0)/v0,passes=Math.abs(selectorError)<=.012,l=.04,L=.12,accepted=E/ME,trueD=accepted*EB*l*(l+2*L)/(2*v0*v0),measuredD=Math.round(trueD/.0001)*.0001,ratio=2*measuredD*vSelected*vSelected/(EB*l*(l+2*L)),p={l:45,r:24,t:34,b:42},mid=h*.58,xA=p.l+(w-p.l-p.r)*.08,xB=p.l+(w-p.l-p.r)*.39,xC=p.l+(w-p.l-p.r)*.64,xD=w-p.r,selectorBend=Math.max(-h*.20,Math.min(h*.20,selectorError*h*.65)),yAtA=mid+selectorBend,yEnd=mid-trueD/.04*(h*.30);ctx.fillStyle="rgba(55,107,140,.08)";ctx.fillRect(xA,p.t,xB-xA,h-p.t-p.b);ctx.fillStyle="rgba(166,75,53,.08)";ctx.fillRect(xB,p.t,xC-xB,h-p.t-p.b);ctx.strokeStyle=colors.ink;ctx.lineWidth=2;[xA,xB,xC,xD].forEach(x=>{ctx.beginPath();ctx.moveTo(x,p.t);ctx.lineTo(x,h-p.b);ctx.stroke()});ctx.strokeStyle=passes?colors.green:colors.rust;ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(p.l,mid);ctx.quadraticCurveTo((xA+xB)/2,mid,xB,yAtA);if(passes){for(let i=0;i<=100;i++){const u=i/100,x=xB+u*(xC-xB),y=mid+(yEnd-mid)*(l/(l+2*L))*u*u;ctx.lineTo(x,y)}ctx.lineTo(xD,yEnd)}ctx.stroke();ctx.fillStyle=colors.muted;ctx.fillText("A: tune magnitudes E(yA) and B(z)",xA+5,p.t+17);ctx.fillText(`B: magnitude E(yB) = ${format(EB,0)} V/m`,xB+8,p.t+17);ctx.fillText("l = 4,00e−2 m (fixed)",xB+8,h-p.b-28);ctx.fillText("L = 1,20e−1 m (fixed)",xC+5,h-p.b-12);ctx.fillText(passes?"beam horizontal — passes slit":"beam misses slit",xA+12,h-p.b-12);if(passes)ctx.fillText(`d = ${sci(measuredD,3)} m`,xD-110,yEnd-10);set("speed",passes?`${sci(vSelected,4)} m/s`:"—");set("deflection",passes?`${sci(measuredD,4)} m`:"—");set("ratio",passes?`${sci(ratio,4)} C/kg`:"—")}

  const millikan={phase:"ready",progress:0,drop:null,vf:null,vr:null,histogram:null,animation:0};
  function newMillikanDrop(s){const eta=1.81e-5,rho=860,g=9.80665,d=s.plateGap*1e-3,V=s.plateVoltage,n=1+Math.floor(Math.random()*9),fieldScale=Math.sqrt((V/d)/105000),maxRadius=Math.max(.18,.65*fieldScale),minRadius=Math.max(.08,maxRadius*.55),R=(minRadius+Math.random()*(maxRadius-minRadius))*1e-6,vf=2*rho*g*R*R/(9*eta),factor=6*Math.PI*d/V*Math.sqrt(9*eta**3/(2*rho*g))*Math.sqrt(vf),vr=n*E/factor-vf;return{n,R,vf,vr}}
  function measured(value){return value*(1+(Math.random()-.5)*.012)}
  function chargeFromSpeeds(s,vf,vr){const eta=1.81e-5,rho=860,g=9.80665,d=s.plateGap*1e-3,V=s.plateVoltage;return 6*Math.PI*d/V*Math.sqrt(9*eta**3/(2*rho*g))*(vr+vf)*Math.sqrt(vf)}
  function drawMillikan(s){
    const{ctx,width:w,height:h}=fit();
    if(millikan.histogram){
      const samples=millikan.histogram,values=samples.map(item=>item.q/1e-19),bins=45,min=.5,max=15.5,counts=Array(bins).fill(0);
      values.forEach(value=>{const i=Math.max(0,Math.min(bins-1,Math.floor((value-min)/(max-min)*bins)));counts[i]++});
      const means=[];
      for(let n=1;n<=9;n++){const group=samples.filter(item=>item.n===n);if(group.length)means.push(group.reduce((sum,item)=>sum+item.q,0)/group.length)}
      const spacings=means.slice(1).map((mean,i)=>mean-means[i]),estimatedE=spacings.reduce((sum,value)=>sum+value,0)/Math.max(1,spacings.length),p={l:52,r:18,t:42,b:45},peak=Math.max(...counts,1);
      axes(ctx,w,h,p,"q (10⁻¹⁹ C)","drops");
      counts.forEach((count,i)=>{const x=p.l+i/bins*(w-p.l-p.r),bw=(w-p.l-p.r)/bins-1,bh=count/peak*(h-p.t-p.b)*.82;ctx.fillStyle=colors.green;ctx.fillRect(x,h-p.b-bh,bw,bh)});
      ctx.fillStyle=colors.muted;
      for(let n=1;n<=9;n++){const value=n*estimatedE/1e-19,x=p.l+(value-min)/(max-min)*(w-p.l-p.r);ctx.strokeStyle=colors.rust;ctx.setLineDash([3,3]);ctx.beginPath();ctx.moveTo(x,p.t);ctx.lineTo(x,h-p.b);ctx.stroke();ctx.setLineDash([]);ctx.fillText(format(value,1),x-8,h-p.b+18)}
      ctx.fillStyle=colors.ink;ctx.fillText(`500 drops · mean peak spacing Δq = ${sci(estimatedE,3)} C`,p.l+8,18);ctx.fillText("Adjacent peaks differ by one elementary charge: e = Δq",p.l+8,34);
      set("fallMeasured","500 measurements");set("riseMeasured","500 measurements");set("charge","Histogram of q");set("nearest",`e = Δq = ${sci(estimatedE,3)} C`);return;
    }
    const top=70,bottom=h-60,cx=w/2,position=millikan.phase==="falling"?top+(bottom-top)*millikan.progress:millikan.phase==="rising"?bottom-(bottom-top)*millikan.progress:millikan.phase==="fallen"||millikan.phase==="measured"?bottom:top;
    ctx.strokeStyle=colors.ink;ctx.lineWidth=4;ctx.beginPath();ctx.moveTo(55,45);ctx.lineTo(w-55,45);ctx.moveTo(55,h-40);ctx.lineTo(w-55,h-40);ctx.stroke();ctx.fillStyle=colors.gold;ctx.beginPath();ctx.arc(cx,position,16,0,2*Math.PI);ctx.fill();ctx.fillStyle=colors.muted;ctx.fillText("same oil drop",cx-42,28);
    const fallDisplay=millikan.phase==="falling"&&millikan.drop?millikan.drop.vf:millikan.vf,riseDisplay=millikan.phase==="rising"&&millikan.drop?millikan.drop.vr:millikan.vr,labelX=Math.min(w-175,cx+25),labelY=Math.max(78,Math.min(h-72,position));
    if(fallDisplay!=null){ctx.fillStyle=colors.rust;ctx.fillText(`v_f = ${format(fallDisplay*1e3,3)} mm/s`,labelX,labelY-8)}
    if(riseDisplay!=null){ctx.fillStyle=colors.green;ctx.fillText(`v_r = ${format(riseDisplay*1e3,3)} mm/s`,labelX,labelY+10)}
    ctx.fillStyle=colors.muted;ctx.fillText(millikan.phase==="falling"?"field off — observe terminal fall":millikan.phase==="rising"?"field on — observe terminal rise":millikan.phase==="fallen"?"fall measured — now turn the field on":millikan.phase==="measured"?"both speeds measured — calculate q":"start by observing the fall",65,h-14);
    const q=millikan.vf!=null&&millikan.vr!=null?chargeFromSpeeds(s,millikan.vf,millikan.vr):null,n=q/E;
    set("fallMeasured",millikan.vf==null?"—":`${format(millikan.vf*1e3,3)} mm/s`);set("riseMeasured",millikan.vr==null?"—":`${format(millikan.vr*1e3,3)} mm/s`);set("charge",q==null?"—":`${sci(q,4)} C`);set("nearest",q==null?"—":`q/e = ${format(n,3)} → n = ${Math.round(n)}`)
  }
  function drawDiffraction(s){const{ctx,width:w,height:h}=fit(),p={l:52,r:20,t:24,b:38},lambda=H/Math.sqrt(2*ME*E*s.electronVoltage),arg=s.diffractionOrder*lambda/(2*s.latticeSpacing*1e-9),theta=arg<=1?Math.asin(arg):NaN;axes(ctx,w,h,p,"θ (degrees)","Intensity");ctx.strokeStyle=colors.blue;ctx.lineWidth=2.5;ctx.beginPath();for(let i=0;i<=720;i++){const deg=90*i/720,I=Number.isFinite(theta)?Math.exp(-.5*((deg-theta*180/Math.PI)/1.5)**2):0,px=p.l+i/720*(w-p.l-p.r),py=h-p.b-I*(h-p.t-p.b)*.88;i?ctx.lineTo(px,py):ctx.moveTo(px,py)}ctx.stroke();set("wavelength",`${format(lambda*1e9,4)} nm`);set("momentum",`${(H/lambda).toExponential(3)} kg·m/s`);set("angle",Number.isFinite(theta)?`${format(theta*180/Math.PI,2)}°`:"No allowed peak");set("ring",Number.isFinite(theta)?`${format(2*theta*180/Math.PI,2)}°`:"—")}
  function drawSemiclassical(s){const{ctx,width:w,height:h}=fit(),p={l:58,r:22,t:24,b:38},n=s.quantumN,scale=s.systemScale;axes(ctx,w,h,p,"state n","Energy");let energy,spacing,size;if(s.model==="well"){const L=scale*1e-9,fn=k=>k*k*H*H/(8*ME*L*L)/E;energy=fn(n);spacing=fn(n+1)-energy;size=`L = ${format(scale,2)} nm`}else if(s.model==="oscillator"){const nu=scale*1e15,fn=k=>k*H*nu/E;energy=fn(n);spacing=H*nu/E;size=`ν = ${format(scale,2)} PHz`}else{const fn=k=>-13.605693*scale*scale/(k*k);energy=fn(n);spacing=fn(n+1)-energy;size=`rₙ = ${format(.0529177*n*n/scale,3)} nm`}const vals=[];for(let k=1;k<=8;k++){if(s.model==="well")vals.push(k*k*energy/(n*n));else if(s.model==="oscillator")vals.push(k*energy/n);else vals.push(-13.605693*scale*scale/(k*k))}const min=Math.min(...vals),max=Math.max(...vals),map=e=>h-p.b-(e-min)/(max-min||1)*(h-p.t-p.b)*.85;vals.forEach((en,i)=>{ctx.strokeStyle=i+1===n?colors.green:colors.grid;ctx.lineWidth=i+1===n?4:1.5;ctx.beginPath();ctx.moveTo(p.l,map(en));ctx.lineTo(w-p.r,map(en));ctx.stroke();ctx.fillStyle=colors.muted;ctx.fillText(`n=${i+1}`,p.l+5,map(en)-3)});set("energy",`${format(energy,4)} eV`);set("action",`${n}h = ${(n*H).toExponential(3)} J·s`);set("size",size);set("spacing",`${format(spacing,4)} eV`)}
  const drawers={"blackbody-radiation":drawBlackbody,"double-slit":drawSlit,"hydrogen-bohr":drawHydrogen,"thomson-circle":drawThomsonCircle,"thomson-deflection":drawThomsonDeflection,"millikan-oil-drop":drawMillikan};
  function render(){const s=state();updateOutputs(s);drawers[slug](s)}
  function animateMillikan(phase,done){cancelAnimationFrame(millikan.animation);millikan.phase=phase;millikan.progress=0;millikan.histogram=null;const start=performance.now(),duration=1400;const tick=now=>{millikan.progress=Math.min(1,(now-start)/duration);render();if(millikan.progress<1)millikan.animation=requestAnimationFrame(tick);else{done();render()}};millikan.animation=requestAnimationFrame(tick)}
  if(slug==="millikan-oil-drop"){
    $("measureFallButton").addEventListener("click",()=>{const s=state();millikan.drop=newMillikanDrop(s);millikan.vf=null;millikan.vr=null;animateMillikan("falling",()=>{millikan.vf=measured(millikan.drop.vf);millikan.phase="fallen"})});
    $("measureRiseButton").addEventListener("click",()=>{if(!millikan.drop||millikan.vf==null)return;animateMillikan("rising",()=>{millikan.vr=measured(millikan.drop.vr);millikan.phase="measured"})});
    $("runHundredButton").addEventListener("click",()=>{const s=state();cancelAnimationFrame(millikan.animation);millikan.histogram=Array.from({length:500},()=>{const drop=newMillikanDrop(s);return{q:chargeFromSpeeds(s,measured(drop.vf),measured(drop.vr)),n:drop.n}});millikan.phase="histogram";render()});
  }
  cfg.controls.forEach(c=>$(c.id).addEventListener("input",()=>{if(slug==="millikan-oil-drop"){millikan.phase="ready";millikan.drop=null;millikan.vf=null;millikan.vr=null;millikan.histogram=null}render()}));$("resetButton").addEventListener("click",()=>{cfg.controls.forEach(c=>{$(c.id).value=c.value});if(slug==="millikan-oil-drop"){cancelAnimationFrame(millikan.animation);millikan.phase="ready";millikan.drop=null;millikan.vf=null;millikan.vr=null;millikan.histogram=null}render()});window.addEventListener("resize",render);render();
})();
